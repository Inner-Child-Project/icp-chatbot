services:
  chatbot:
    build: .
    ports:
      - "8100:8100"
    environment:
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - OPENROUTER_MODEL=${OPENROUTER_MODEL:-openai/gpt-4o-mini}
      - LANGSMITH_API_KEY=${LANGSMITH_API_KEY}
      - LANGSMITH_TRACING=${LANGSMITH_TRACING:-true}
      - LANGSMITH_PROJECT=${LANGSMITH_PROJECT:-icp-chatbot}
      - N8N_LEAD_WEBHOOK_URL=${N8N_LEAD_WEBHOOK_URL:-http://n8n:5678/webhook/icp-lead}
      - CORS_ORIGINS=${CORS_ORIGINS:-https://leadforge.innerchildproject.us,http://localhost:4321}
      - RATE_LIMIT=${RATE_LIMIT:-20}
      - RATE_LIMIT_WINDOW=${RATE_LIMIT_WINDOW:-60}
    volumes:
      - chatbot-data:/app/data
    networks:
      - icp-net
    restart: unless-stopped

volumes:
  chatbot-data:

networks:
  icp-net:
    external: true
